import PixelArtGalleryClient from './Client';

export const metadata = {
  title: 'Pixel Art Gallery — PearlOS',
  description: 'Generate and browse pixel art icons, buttons, badges, and decorative UI elements',
};

export default function PixelArtGalleryPage() {
  return <PixelArtGalleryClient />;
}
