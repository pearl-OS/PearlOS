apiVersion: v1
kind: Service
metadata:
  name: dashboard-stg
  namespace: dashboard-stg
  labels:
    app: dashboard-stg
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 4000
    protocol: TCP
    name: http
  selector:
    app: dashboard-stg
