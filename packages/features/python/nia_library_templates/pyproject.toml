[project]
name = "nia-library-templates"
version = "0.1.0"
description = "Auto-generated library template metadata for Nia Universal HTML generation"
readme = "README.md"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["nia_library_templates"]
