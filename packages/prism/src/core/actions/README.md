# Actions Folder

This folder contains business logic functions ("actions") for manipulating and retrieving core data entities. Actions encapsulate operations such as CRUD, workflow steps, and domain-specific logic, and are used by both apps and APIs to ensure consistency and reusability. 