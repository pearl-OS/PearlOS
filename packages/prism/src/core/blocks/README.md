# Blocks Folder

This folder defines the TypeScript classes and interfaces for all core data models ("blocks") used in the backend. Each block represents a specific entity (e.g., user, agenda, activity) and provides type-safe schemas and helpers for data manipulation. 