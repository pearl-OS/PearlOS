# Components Folder

This folder contains reusable UI components and utilities shared across apps. Components are written in TypeScript/React and are designed for composability and consistency in the user interface. 